# Xcode
build/
DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout

# XcodeGen generates these from project.yml — never commit them
MLBValueBets.xcodeproj/
MLBValueBets.xcworkspace/

# Swift Package Manager
.build/
.swiftpm/
Packages/
Package.resolved
# (leave Package.swift tracked if we add one later)

# CocoaPods (not used, but safe to exclude)
Pods/
*.xcworkspace

# Carthage (not used)
Carthage/Build/
Carthage/Checkouts/

# fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# Xcode Playgrounds
timeline.xctimeline
playground.xcworkspace

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
$RECYCLE.BIN/

# Editors
.idea/
.vscode/
*.swp
*.swo
*~

# Secrets — never commit
*.p8
*.p12
*.mobileprovision
*.cer
GoogleService-Info.plist
.env
.env.local
secrets.xcconfig

# Local scratch: downloaded CI snapshot artifacts + parse-check output
.tmp-snapshots/
ui-stdout.txt
ui-stderr.txt
typecheck-stdout.txt
typecheck-stderr.txt
new-snapshots/
